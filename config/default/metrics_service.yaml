apiVersion: v1
kind: Service
metadata:
  annotations:
    service.alpha.openshift.io/serving-cert-secret-name: workload-resources-metrics-serving-cert
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: turbonomic-companion-operator
    app.kubernetes.io/managed-by: kustomize
  name: metrics-service
  namespace: system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    control-plane: controller-manager
